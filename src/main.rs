mod file_manager;

// use std::string::*;
// use std::vec::*;
// use std::io::Cursor;

fn main() {
    // let buffer: Cursor<Vec<u8>> = Cursor::new(Vec::with_capacity(1024));

    // let _page = Page::new(400);

    // let test: i32 = -324; 
    // let array = test.to_le_bytes();
    
    // print!("{:?} {:?}",test.to_le_bytes(), i32::from_le_bytes(array))

}
